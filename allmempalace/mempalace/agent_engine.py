SYSTEM_PROMPT = """
Ты — Интегральный Агент (Зеркало Субъектности). 
Твоя цель: Перевод пользователя из состояния «Жертвы» в состояние «Хозяина Смыслов».
Методология: Спиральная Динамика, AQAL Уилбера, Когнитивные стадии Пиаже.

ПРОТОКОЛЫ:
1. Закон Разрыва: Подсвечивай несоответствия между словами и действиями.
2. Закон Тени: Если пользователь избегает темы 0-4 лет — это дефицит безопасности.
3. Анализируй маркеры: "всегда", "никто" (искажения), "мне всё равно" (подавление).

Твой ответ всегда должен содержать:
1. Текстовое сообщение (Зеркало).
2. JSON-блок для обновления паттернов (confidence от 0.0 до 1.0).
"""

def process_message(user_input, current_state):
    # Здесь происходит магия LLM. 
    # Модель должна проанализировать user_input на основе current_state.
    # Для примера имитируем логику "Детектора псевдо-проработки":
    
    updates = []
    if "я понимаю" in user_input.lower() and "но" not in user_input.lower():
        # Подозрение на интеллектуализацию (из README п. 8.6)
        updates.append({
            "pattern": "intellectualization",
            "confidence": 0.85,
            "trigger": "self_reflection"
        })
    
    return "Инсайт: Я замечаю, как вы переводите эмоциональный опыт в сухую логику...", updates

#Ваш следующий шаг: Чтобы система начала выдавать глубокие инсайты, нужно подключить agent_engine.py к API любой большой языковой модели, передав ей SYSTEM_PROMPT и текущий state.json.

#Нужно ли мне помочь с написанием кода для подключения конкретного API (например, OpenAI или Anthropic)?